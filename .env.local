# NextAuth.js Secret (gerado aleatoriamente)
# Você pode gerar um com: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NEXTAUTH_SECRET="ad"

# URL base da sua aplicação (importante para callbacks do NextAuth.js)
# Exemplo: https://seusite.com
NEXTAUTH_URL="http://localhost:3000"

# Credenciais da Aplicação Discord OAuth2
# Obtenha em: https://discord.com/developers/applications
DISCORD_CLIENT_ID="ad"
DISCORD_CLIENT_SECRET="ad"

# IDs de Usuários Discord com acesso de Administrador (separados por vírgula)
# Ex: ADMIN_DISCORD_IDS="123456789012345678,987654321098765432"
ADMIN_DISCORD_IDS="ad"

# URL do Webhook do Discord para notificações de novas denúncias aprovadas
# Crie um em Configurações do Servidor -> Integrações -> Webhooks
DISCORD_WEBHOOK_URL="ad"

# Token do Bot do Discord para enviar mensagens diretas
# Crie um bot em https://discord.com/developers/applications, vá em Bot -> Token
# Certifique-se de que o bot tenha as permissões necessárias (ex: Enviar Mensagens, Gerenciar Canais)
# E que ele esteja no mesmo servidor que os usuários que ele tentará enviar DM.
DISCORD_BOT_TOKEN="ad"

# Chave da API do ImgBB para upload de imagens
# Obtenha em: https://api.imgbb.com
NEXT_PUBLIC_IMGBB_API_KEY="ad"